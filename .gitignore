# Personal development values (not committed)
values-dev.yaml
values-pubsub.yaml
values-dev-pubsub.yaml

# Helm lock files (regenerated by helm dependency update)
Chart.lock
charts/*/Chart.lock

# Helm dependency packages (downloaded charts stored in each chart's charts/ dir)
charts/*/charts/
*.tgz
