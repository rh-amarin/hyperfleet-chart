# Personal development values (not committed)
values-dev.yaml
values-pubsub.yaml
values-dev-pubsub.yaml

# Helm lock file (regenerated by helm dependency update)
Chart.lock

# Helm dependency packages (downloaded charts)
charts/

# Work in progress notes
WORK_IN_PROGRESS.md
